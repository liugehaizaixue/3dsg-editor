export declare function measureTextSize(text: string, font: string): {
    width: number;
    height: number;
};
export declare function measureTextArraySize(textArr: Array<string>, font: string): {
    width: number;
    height: number;
};
